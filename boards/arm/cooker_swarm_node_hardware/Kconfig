# Kconfig - cooker_swarm_node_hardware

if BOARD_COOKER_SWARM_NODE_HARDWARE

config BOARD_ENABLE_DCDC
        bool "Enable DCDC mode"
        select SOC_DCDC_NRF52X
        default y

endif # BOARD_COOKER_SWARM_NODE_HARDWARE
